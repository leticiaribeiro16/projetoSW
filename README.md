Para rodar o servidor (API Http), coloque o seguinte comando no terminal:

<code>json-server --watch db.json</code>

Para rodar o client (front-end), coloque o seguinte comando no terminal:

<code>npm run dev</code>
